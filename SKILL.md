---
name: obsidian-benchmark-content-importer
description: 将小红书图文/视频笔记和微信公众号文章从分享链接采集、转写、OCR、下载媒体并按统一目录导入 Obsidian。用于用户粘贴小红书或公众号链接并要求保存、收录、归档、导入知识库、提取视频文案或图片文字，也用于查重、补全或验证既有对标内容条目；当前正式支持小红书与公开微信公众号文章，抖音和 YouTube 需先做单链接试跑。
---

# Obsidian 对标内容导入

将链接对应内容尽量原样保存为可追溯的 Obsidian 资料。默认不总结、不仿写、不分析；用户同时要求知识摄入时，再遵循目标库自身的 `AGENTS.md` 与结构规范生成摘要、连接和选题。

## 开始前

1. 确认目标 Obsidian Vault。优先读取 Obsidian 配置中的当前打开库；存在多个库或用户点名库时，以用户明确指定为准。
2. 从目标库根目录向上检查 `AGENTS.md`，完整读取适用规范。若库规定“待处理→原始资料→知识层”，遵循该流程，不另建平行体系。
3. 查找链接中的平台与内容 ID；在目标范围内搜索完整链接和内容 ID。重复时不要新建或覆盖，除非用户明确要求更新。
4. 若用户只要求试跑，把结果写入临时或交付目录，不写正式知识库。

详细的平台步骤见 [references/platform-workflows.md](references/platform-workflows.md)，统一字段与目录见 [references/output-schema.md](references/output-schema.md)。

## 工作流

### 1. 识别平台

- `xiaohongshu.com`：小红书。
- `mp.weixin.qq.com`：微信公众号文章。
- 抖音、YouTube：标记为“实验支持”，先用一个真实链接验证元数据、媒体、转写和落盘，未经验证不要宣称正式支持。
- 其他平台：说明暂未适配，不套用错误解析器。

### 2. 获取来源

优先使用能够复用用户登录状态的浏览器。公开页面可直接下载 HTML；遭遇登录、验证码、空壳页面或动态签名时，切换到用户已登录浏览器读取。不得绕过访问控制，不保存 Cookie、Token 或账号凭据。

可将页面 HTML 交给脚本生成中间清单：

```bash
python scripts/parse_wechat.py page.html manifest.json
python scripts/parse_xiaohongshu.py page.html manifest.json --source-url '<分享链接>'
```

脚本只解析已获得的页面，不负责绕过登录或访问限制。

解析后下载清单中的远程媒体；脚本会先写 `.part`、根据文件头修正图片扩展名，再回写本地附件清单：

```bash
python scripts/download_assets.py manifest.json --staging-dir '<临时附件目录>'
```

### 3. 保存媒体和文字

- 下载封面、正文图片和主内容视频；临时写入 `.part`，校验非空及真实类型后再定稿。
- 优先使用平台原始字幕。没有字幕时，对视频音轨做语音转写。
- 最终视频文字不保留时间戳，清理为连续、可读的段落；保留原意，不擅自补写。
- 图片只提取具有增量知识的信息。车标、水印、装饰字、与正文重复的车型名不单独堆入 OCR 文稿。
- 已有完整字幕时，默认不生成“视频画面文字”；只有画面承载口播未提到的表格、步骤、参数等关键信息时，才并入唯一的“视频文字/图片文字”板块。
- 公众号文章在明确的正文结束标记后停止，排除推荐文章、广告卡片和推荐视频。

### 4. 生成条目

准备符合 [references/output-schema.md](references/output-schema.md) 的 JSON 清单后运行：

```bash
python scripts/build_entry.py manifest.json --output-dir '<目标分类目录>'
```

目录名固定为 `内容标题_平台_内容ID`；根层级只能有 `笔记信息.md` 与 `附件/`。不存在的媒体不创建空文件。

主笔记必须满足：

- 全部约定字段只出现一次。
- 完整正文直接出现在 `笔记信息.md`。
- 视频文字与有效图片文字直接出现在唯一的 `## 视频文字/图片文字` 下，同时可在附件保留独立备份。
- 获取不到的互动数据写“平台未公开”或“未获取”，绝不写成 `0`。
- 图片和视频使用 Obsidian 原生嵌入 `![[附件/文件名]]`。

### 5. 验证

```bash
python scripts/validate_entry.py '<内容目录>'
```

必须检查：

- 根目录结构正确；
- 必填字段、内容 ID 和来源链接存在；
- “视频文字/图片文字”板块只有一个；
- 所有本地嵌入均可解析；
- 媒体非空且扩展名与真实类型相符；
- 公众号推荐内容未混入正文；
- 重复内容没有被新建或覆盖。

若目标库要求知识摄入，继续更新来源摘要、素材清单、知识地图、日志、内容规律和选题；单篇外部样本只能记为“样本观察”，不能直接升级为稳定规律或用户本人证据。

## 结果报告

完成后简洁报告：

- 内容标题、平台和保存路径；
- 图片成功/失败数量；
- 视频及转写状态；
- 图片文字状态；
- 未获取字段；
- 是否命中重复；
- 若执行了知识摄入，列出更新的知识页与内容系统页面。

遇到访问限制时，明确说明需要用户在浏览器登录、提供导出文件或上传视频；不要把半成品标记为成功。
